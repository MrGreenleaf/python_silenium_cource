=== НАЧАЛО ФАЙЛА: module\_0\_testing\_principles.md ===
# **МОДУЛЬ 0: ФИЛОСОФИЯ И ПРИНЦИПЫ АВТОМАТИЗИРОВАННОГО ТЕСТИРОВАНИЯ**
## **От теории к практике: как думать о тестах правильно**
Продолжительность: 1–2 дня\
Цель: Понять фундаментальные принципы, чтобы писать стабильные тесты, а не копировать код

-----
## **0.1 Зачем нужны автотесты?**
## **Ручное тестирование vs Автоматизация**

|**Ручное**|**Автоматизация**|
| :- | :- |
|Медленно (1 тест = 2 мин)|Быстро (1 тест = 2 сек)|
|Устает повторять|Не устает|
|Пропускает баги|Находит каждый раз|
|Дорого масштабировать|Дешево масштабировать|
## **ROI автотестов**
Автоматизируй когда:

- Регрессия (проверять старый функционал)
- Нагрузка (100+ сценариев)
- Критичный путь (оплата, авторизация)

НЕ автоматизируй:

- Одноразовые проверки
- Дизайн/UX (кнопка красивая?)
- Исследовательское тестирование
## **Где UI-тесты в пирамиде**
text

Testing Pyramid (Martin Fowler):

`          `/\

`         `/  \   UI-тесты (5-10%)

`        `/    \

`       `/      \ Integration (15-20%)

`      `/\_\_\_\_\_\_\_\_\

`     `Unit тесты (70%)

\


UI-тесты = вершина пирамиды: медленные, хрупкие, но проверяют реальный пользовательский путь.

-----
## **0.2 AAA (Arrange-Act-Assert) - золотое правило**
## **❌ ПЛОХО: всё в куче**
python

def test\_login():

`    `driver = webdriver.Chrome()

`    `driver.get("https://example.com")

`    `driver.find\_element("id", "username").send\_keys("admin")

`    `driver.find\_element("id", "password").send\_keys("secret")

`    `driver.find\_element("id", "login").click()

`    `time.sleep(2)

`    `if "dashboard" in driver.current\_url:

`        `print("Успех")

`    `else:

`        `print("Провал")

`    `driver.quit()

\


Проблемы:

- Что тестируется? Неясно
- Если упадет на 3-м шаге — непонятно где баг
- sleep(2) — флаки тест
- if/else внутри — не детерминировано
## **✅ ХОРОШО: четкое разделение AAA**
python

def test\_login():

`    `*# ARRANGE - подготовка условий*

`    `driver = webdriver.Chrome()

`    `driver.get("https://example.com/login")



`    `*# ACT - выполняем тестируемое действие*

`    `driver.find\_element("id", "username").send\_keys("admin")

`    `driver.find\_element("id", "password").send\_keys("secret")

`    `driver.find\_element("id", "login").click()



`    `*# ASSERT - проверяем результат*

`    `assert "dashboard" in driver.current\_url

`    `driver.quit()

\


Почему AAA лучше:

text

ПЛОХО: 1 тест = 7 строк непонятного кода

ХОРОШО: 3 блока, каждый делает одно дело

\
\-----
## **0.3 Принципы хороших автотестов**
## **1. Независимость тестов**
❌ ПЛОХО: тесты зависят друг от друга

python

def test\_create\_user():

`    `*# Создает пользователя*

\


def test\_login\_with\_that\_user():  *# ← Зависит от test\_create\_user!*

`    `*# Логин с пользователем из предыдущего теста*

\


Проблема: Запуск в случайном порядке = флаки тесты

✅ ХОРОШО: каждый тест самодостаточен

python

def test\_login\_valid\_user():

`    `*# Создает своего пользователя*

`    `user = create\_test\_user()

`    `login(user)  *# Работает всегда*

\

## **2. Атомарность теста**
❌ ПЛОХО: много проверок в одном тесте

python

def test\_user\_profile():

`    `*# Проверяем Имя И email И телефон И аватар И настройки*

`    `assert name == "John"

`    `assert email == "john@example.com"

`    `assert phone == "+123"

`    `*# Если упадет на 3-м → непонятно что сломано*

\


✅ ХОРОШО: один тест = одна идея

python

def test\_user\_name\_displayed():

`    `assert name == "John"

\


def test\_user\_email\_displayed():

`    `assert email == "john@example.com"

\

## **3. Детерминированность**
❌ ПЛОХО: случайные данные

python

username = f"user{random.randint(1, 1000)}"  *# Разный результат каждый раз*

\


✅ ХОРОШО: предсказуемые данные

python

username = "test\_user\_001"  *# Всегда тот же результат*

\

## **4. Читаемость = тест как документация**
❌ ПЛОХО:

python

def test\_1(): ...

def test\_2(): ...

\


✅ ХОРОШО:

python

def test\_user\_can\_login\_with\_valid\_credentials(): ...

def test\_locked\_user\_cannot\_login(): ...

\


Тест читаешь → понимаешь, как работает система.

-----
## **0.4 Антипаттерны и флаки-тесты**
## **Красные флаги в коде:**
text

❌ time.sleep()           → Замедляет + флаки

❌ random данные         → Нельзя воспроизвести баг

❌ База данных не чистится → Тесты мешают друг другу

❌ if/else в тестах      → Разное поведение

❌ Тест > 100 строк      → Непонятно что проверяет

\

## **Типовые причины флаки-тестов:**
1. Race conditions — не дождался загрузки элемента
1. Зависимости — тест A сломал данные для теста B
1. Внешние сервисы — платежка упала
1. Окружение — тестовая БД перезагрузилась
-----
## **0.5 Управление тестовыми данными**
## **Правильно:**
python

@pytest.fixture

def test\_user():

`    `"""Создает чистого пользователя для теста"""

`    `user = User(name="test", email="test@example.com")

`    `db.save(user)

`    `yield user

`    `db.delete(user)  *# Чистка после теста*

\

## **Неправильно:**
python

user = User(name="admin")  *# Захардкожено в коде*

\
\-----
## **🧪 ПРАКТИЧЕСКИЕ ЗАДАНИЯ**
## **Задание 0.1: Анализ плохих тестов**
text

Дан плохой тест. Найди 5 нарушений принципов:

def test\_profile():

`    `driver.get("/profile")

`    `time.sleep(3)

`    `if driver.find\_elements(".name"):

`        `assert driver.find\_element(".name").text == "John"

`    `else:

`        `driver.refresh()

`        `time.sleep(2)

`        `assert driver.find\_element(".name").text == "John"

\

## **Задание 0.2: Переписать по AAA**
Перепиши плохой тест по принципам AAA + независимость.
## **Задание 0.3: Дизайн тестов**
Спроектируй 5 независимых тестов для функционала "Регистрация пользователя".

-----
## **✅ Чек-лист после модуля**
Студент должен уметь:

- ` `Объяснить AAA и показать на примере
- ` `Найти антипаттерны в тесте
- ` `Написать независимый тест
- ` `Понять, когда автотесты выгодны

Переход к Модулю 1: теперь ты знаешь КАК писать тесты правильно!

=== КОНЕЦ ФАЙЛА: module\_0\_testing\_principles.md ===

Готово! Скачай файл и скажи "ДАЛЕЕ" для Модуля 7.5 про мокирование. 🚀

Подробнее в модуле, почему хорошо, почему плохо, что за принцип, для чего он.

=== НАЧАЛО ФАЙЛА: module\_0\_testing\_principles.md (РАСШИРЕННАЯ ВЕРСИЯ) ===
# **МОДУЛЬ 0: ФИЛОСОФИЯ И ПРИНЦИПЫ АВТОМАТИЗИРОВАННОГО ТЕСТИРОВАНИЯ**
## **Почему 90% автотестов флакают и как этого избежать**
Продолжительность: 2 дня\
Цель: Освоить 5 фундаментальных принципов, которые спасут твои тесты от "моргания"

-----
## **0.1 Зачем нужны автотесты?**
## **Ручное vs Автоматизированное**

|**Аспект**|**Ручное тестирование**|**Автоматизированное**|
| :- | :- | :- |
|Скорость|1 тест = 2 минуты|1 тест = 2 секунды|
|Масштаб|10 тестов в день|10 000 тестов в день|
|Надежность|Забывает проверить|Проверяет каждый раз|
|Стоимость|$50/час × 100 тестов = $5000|$0.01/тест × 1000 = $10|
|Когда использовать|UX, исследование|Регрессия, дымовые тесты|

ROI правило: Автоматизируй если тест повторяется >5 раз в месяц.
## **Testing Pyramid**
text

`          `UI тесты (5%)

`         `/        \

`        `/ Integration (15%)

`       `/

Unit тесты (80%)

\


UI-тесты медленные и хрупкие → используй их только для критичных сценариев.

-----
## **0.2 AAA (Arrange-Act-Assert)**
## **Что это такое?**
AAA = четкое разделение ответственности:

text

ARRANGE = Подготовить мир для теста

ACT = Выполнить тестируемое действие  

ASSERT = Проверить результат

\

## **❌ ПЛОХО: "Спагетти-тест"**
python

def test\_login():

`    `driver = webdriver.Chrome()

`    `driver.get("https://example.com")

`    `driver.find\_element("id", "username").send\_keys("admin")

`    `driver.find\_element("id", "password").send\_keys("secret")

`    `driver.find\_element("id", "login").click()

`    `time.sleep(2)

`    `if "dashboard" in driver.current\_url:

`        `print("Успех")

`    `else:

`        `print("Провал")

`    `driver.quit()

\


Почему плохо:

1. Неясно что тестируется — авторизация? Загрузка страницы? Навигация?
1. time.sleep(2) — флаки тест (иногда 1.5с хватает, иногда 3с мало)
1. if/else — тест должен всегда падать одинаково при баге
1. Смешаны 3 ответственности — непонятно где искать проблему
1. Нет assert — тест "молча" проходит/проваливается
## **✅ ХОРОШО: Четкий AAA**
python

def test\_user\_can\_login\_with\_valid\_credentials():

`    `*# ARRANGE: подготавливаем условия*

`    `driver = webdriver.Chrome()

`    `driver.get("https://example.com/login")



`    `*# ACT: выполняем действие*

`    `driver.find\_element("id", "username").send\_keys("admin")

`    `driver.find\_element("id", "password").send\_keys("secret")

`    `driver.find\_element("id", "login").click()



`    `*# ASSERT: проверяем результат*

`    `assert "dashboard" in driver.current\_url

`    `driver.quit()

\


Почему хорошо:

1. Название теста = документация — сразу понятно что проверяется
1. 3 четких блока — легко найти где баг
1. Explicit assert — тест всегда падает при ошибке
1. Легко читать — как инструкция
1. Легко дебажить — комментируешь ACT и проверяешь состояние

Принцип: *Тест = спецификация поведения системы.*

-----
## **0.3 Принцип 1: Независимость тестов**
## **Зачем нужен?**
Цель: Любой тест можно запустить сам по себе в любом порядке.
## **❌ ПЛОХО: Зависимые тесты**
python

def test\_create\_user():

`    `*# Создает пользователя ID=123*

`    `api.post("/users", {"name": "John"})

\


def test\_login\_with\_created\_user():  *# ← Зависит от test\_create\_user!*

`    `*# Логин пользователя ID=123*

`    `api.post("/login", {"user\_id": 123})

\


Проблемы:

text

pytest test\_create\_user.py     → OK

pytest test\_login\_with\_created\_user.py → FAIL (нет пользователя)

pytest -v                      → FAIL (порядок случайный)

\

## **✅ ХОРОШО: Независимые тесты**
python

def test\_login\_valid\_user():

`    `*# Каждый тест создает СВОИХ данных*

`    `user = api.post("/users", {"name": "test\_user"})

`    `response = api.post("/login", {"user\_id": user["id"]})

`    `assert response.status\_code == 200

\


Почему лучше:

text

pytest любой\_тест.py → Всегда OK

pytest -v → Всегда OK

CI/CD → Стабильно

\


Принцип: *Тесты не должны знать друг о друге.*

-----
## **0.4 Принцип 2: Атомарность**
## **Зачем нужен?**
Один тест = одна идея = одна проверка.
## **❌ ПЛОХО: "Мега-тест"**
python

def test\_user\_profile\_complete():

`    `*# Проверяем 6 вещей сразу!*

`    `assert user.name == "John"

`    `assert user.email == "john@example.com"

`    `assert user.phone == "+123456"

`    `assert user.avatar\_url is not None

`    `assert len(user.settings) == 3

`    `assert user.is\_active == True

\


Проблемы:

text

Тест упал → Что именно сломано? Email? Телефон? Аватар?

Неясно что тестировать

\

## **✅ ХОРОШО: Атомарные тесты**
python

def test\_user\_name\_is\_displayed():

`    `assert user.name == "John"

\


def test\_user\_email\_is\_correct():

`    `assert user.email == "john@example.com"

\


def test\_user\_has\_phone():

`    `assert user.phone == "+123456"

\


Почему лучше:

text

test\_user\_email\_is\_correct → FAIL

→ Немедленно понятно: баг в email

Легче покрыть code coverage

Легче поддерживать

\


Принцип: *Один тест падает по одной причине.*

-----
## **0.5 Принцип 3: Детерминированность**
## **Зачем нужен?**
Одинаковые входы → одинаковые выходы.
## **❌ ПЛОХО: Случайность**
python

import random

\


def test\_search():

`    `query = f"test{random.randint(1, 1000)}"  *# ← Разный запрос!*

`    `results = search(query)

`    `assert len(results) == 10

\


Проблемы:

text

Запуск 1: query="test42" → OK

Запуск 2: query="test777" → FAIL (нет такого)

Невозможно воспроизвести баг

\

## **✅ ХОРОШО: Предсказуемость**
python

def test\_search\_existing\_term():

`    `query = "test\_user\_001"  *# ← Фиксированный запрос!*

`    `results = search(query)

`    `assert len(results) == 1

\


Почему лучше:

text

Всегда тот же результат

Баги воспроизводимы

Легче дебажить

\


Принцип: *Убери random из тестов.*

-----
## **0.6 Принцип 4: Тест как документация**
## **Зачем нужен?**
Тесты читаешь → понимаешь, как работает фича.
## **❌ ПЛОХО: Абстрактные имена**
python

def test\_1(): ...

def test\_something(): ...

def test\_profile\_update():  *# Что именно проверяем?*

\

## **✅ ХОРОШО: Описательные имена**
python

def test\_guest\_cannot\_access\_admin\_panel():

`    `response = client.get("/admin")

`    `assert response.status\_code == 403

\


def test\_admin\_can\_delete\_user():

`    `admin = login("admin")

`    `response = admin.delete("/users/123")

`    `assert response.status\_code == 200

\


Почему лучше:

text

Читаешь test\_guest\_cannot\_access\_admin\_panel()

→ Понимаешь: гости не могут в админку

Без кода понятно логика системы

\


Принцип: *Название теста должно быть полным предложением.*

-----
## **0.7 Принцип 5: Управление данными**
## **Зачем нужен?**
Тесты не должны засорять БД или зависеть от состояния.
## **❌ ПЛОХО: Глобальное состояние**
python

users = []  *# ← Глобальная переменная!*

\


def test\_create\_user():

`    `users.append(create\_user())  *# ← Меняет глобальное состояние*

\

## **✅ ХОРОШО: Фикстуры с чисткой**
python

@pytest.fixture

def fresh\_user():

`    `"""Создает и чистит пользователя"""

`    `user = create\_user()

`    `yield user

`    `delete\_user(user.id)  *# Автоочистка!*

\


Почему лучше:

text

Каждый тест получает чистые данные

Нет конфликтов между тестами

БД всегда в известном состоянии

\


Принцип: *Создавай и чисти свои данные.*

-----
## **🚨 0.8 Антипаттерны (красные флаги)**

|**Антипаттерн**|**Почему плохо**|**Как исправить**|
| :- | :- | :- |
|time.sleep()|Флаки + медленно|Explicit waits|
|random.\*()|Невоспроизводимо|Фиксированные данные|
|if/else в тесте|Разное поведение|Условия в Arrange|
|Тест > 50 строк|Непонятно|Разбить на атомарные|
|Нет assert|"Молча" падает|Всегда assert|
|Данные в коде|Захардкожено|config/фикстуры|

\
**🧪 ПРАКТИЧЕСКИЕ ЗАДАНИЯ**

ВАЖНО: Задания только на понимание принципов. Код писать НЕ нужно!
## **0.1 Анализ антипаттернов (30 мин)**
Дан плохой тест. Найди 5 нарушений принципов:

python

def test\_profile():

`    `driver.get("/profile")

`    `time.sleep(3)

`    `if driver.find\_elements(".name"):

`        `assert driver.find\_element(".name").text == "John"

`    `else:

`        `driver.refresh()

`        `time.sleep(2)

`        `assert driver.find\_element(".name").text == "John"

\


Ответ запиши в таблицу:

|**№**|**Антипаттерн**|**Принцип нарушен**|**Почему плохо**|
| :- | :- | :- | :- |
|1|time.sleep(3)|Детерминированность||
|2|if/else внутри теста|||
|3||Независимость||
|4|Дублирование кода|||
|5||Читаемость||
## **0.2 "Разговор с QA-инженером" (45 мин)**
Ситуация: Коллега-QA написал тест и просит review. Напиши ему комментарии:

Его тест:

text

def test\_cart():

1\. Открыть корзину

2\. Проверить что товаров 0

3\. Добавить товар

4\. Проверить что товаров 1

5\. Удалить товар

6\. Проверить что товаров 0

7\. Закрыть корзину

\


Задание: Напиши список из 4 замечаний по принципам AAA, атомарности, независимости. Укажи, как исправить.

Пример ответа:

text

Замечание 1: Нет четкого AAA. Какой шаг - действие, какой - проверка?

Исправить: Выделить ACT и ASSERT отдельно.

\

## **0.3 Дизайн тестовой стратегии (30 мин)**
Сценарий: "Корзина интернет-магазина". Нужно покрыть автотестами.

Задание: Заполни таблицу — какие принципы нарушаются и как исправить:

|**Ситуация**|**Нарушение принципа**|**Как исправить**|
| :- | :- | :- |
|Тест проверяет добавление товара И удаление товара|||
|Имя теста: test\_cart.py|||
|В тесте random\_product = products[random.choice()]|||
|Тест A создает товар, тест B его удаляет|||
|1 тест проверяет корзину на пустой странице И на странице с товарами|||
## **0.4 "Тест как документация" (20 мин)**
Задание: По названиям тестов определи логику системы. Что проверяет каждый тест?

text

test\_user\_can\_see\_profile()

test\_admin\_can\_delete\_user()

test\_guest\_cannot\_delete\_user()

test\_user\_cannot\_delete\_other\_user()

\


Напиши 1 абзац: "Из этих тестов понятно, что система..."
## **0.5 Чек-лист самопроверки (15 мин)**
Создай свой чек-лист из 10 пунктов. Перед написанием любого теста спрашивай себя:

text

1\. Есть ли четкое AAA?

2\. ...

\
\-----
## **✅ Чек-лист "Что я усвоил"**
- ` `Разобрал 5 антипаттернов в реальном тесте
- ` `Написал review плохого теста
- ` `Диагностировал нарушения в 5 сценариях
- ` `Понял "тест = документация"
- ` `Создал свой чек-лист для будущих тестов

Теперь принципы усвоены → Модуль 1: инструменты!

=== КОНЕЦ ФАЙЛА: module\_0\_testing\_principles.md ===