=== НАЧАЛО ФАЙЛА: module\_2\_web\_tech\_course.md ===
# **МОДУЛЬ 2: ВЕБ-ТЕХНОЛОГИИ И СЕЛЕКТОРЫ (3–4 дня)**
## **Полное объяснение с теорией и примерами**
-----
## **ВВЕДЕНИЕ: КАК РАБОТАЕТ ВЕБ И ОТКУДА БЕРУТСЯ ЭЛЕМЕНТЫ**
Перед тем как искать элементы на странице, нужно понять, как браузер превращает HTML-код в то, что видишь на экране.
## **Путь от кода к экрану**
1. Ты вводишь URL в браузер (например, [**https://google.com**](https://google.com/))
1. Браузер отправляет запрос к серверу Google
1. Сервер отправляет HTML файл (просто текст с тегами)
1. Браузер получает этот текст и начинает его обрабатывать

   Вот пример HTML, который браузер получил:

   xml

   <!DOCTYPE html>

   <html>

   <body>

   `    `<h1>Привет мир</h1>

   `    `<p>Это мой первый сайт</p>

   </body>

   </html>

5. Браузер парсит HTML (разбирает код, понимает структуру)
5. Браузер создает DOM (Document Object Model) — представление структуры в памяти
5. Браузер рисует страницу на экран

   Именно в этом DOM браузер хранит все элементы. И когда ты пишешь тесты Selenium, ты ищешь и взаимодействуешь с элементами в этом DOM.

   -----
   ## **БЛОК 2.1: HTML — ЯЗЫК РАЗМЕТКИ**
   ## **Что такое HTML?**
   HTML (HyperText Markup Language) — это не язык программирования. Это язык разметки, который говорит браузеру, как структурировать содержимое.

   Различие:

- Язык программирования (Python, JavaScript) — имеет логику: if, else, циклы, функции. Выполняет действия.
- Язык разметки (HTML) — просто описывает структуру. Говорит: "это заголовок, это абзац, это кнопка". Никакой логики.
  ## **Почему браузер не показывает тегов?**
  Когда ты видишь сайт в браузере, ты видишь результат работы браузера, а не код. Браузер:

1. Читает HTML теги
1. Интерпретирует их (понимает, что они значат)
1. Рисует результат на экран
1. Скрывает сами теги

   Пример:

   xml

   <p>Привет</p>

   Ты видишь на экране: Привет\
   Ты НЕ видишь: <p> и </p>

   Теги — это инструкции для браузера, а не содержимое для пользователя.
   ## **Структура тега**
   xml

   <tag\_name attribute="value">Содержимое</tag\_name>

   Разбор:

- tag\_name — имя тега (p, div, button, input и т.д.)
- attribute — свойство элемента
- value — значение свойства
- Содержимое — то, что находится внутри тега
- /tag\_name — закрывающий тег

  Примеры:

  xml

  *<!-- Абзац -->*

  <p>Это текст в абзаце</p>

  *<!-- Ссылка с атрибутом href -->*

  <a href="https://google.com">Нажми на меня</a>

  *<!-- Кнопка с атрибутом id -->*

  <button id="submit-btn">Отправить</button>

  *<!-- Input с атрибутами type и name -->*

  <input type="text" name="username">
  ## **Вложение (nesting)**
  Теги могут быть вложены друг в друга. Вложенные теги становятся детьми (children) родительского тега:

  xml

  <div>                    *<!-- Родитель -->*

  `    `<h1>Заголовок</h1>   *<!-- Дитя div -->*

  `    `<p>Текст</p>         *<!-- Дитя div -->*

  </div>

  Здесь <div> — это контейнер, который содержит <h1> и <p>. Важно для понимания структуры при поиске элементов!

  -----
  ## **БЛОК 2.2: DOM (DOCUMENT OBJECT MODEL)**
  ## **Что такое DOM?**
  DOM — это представление HTML-документа в виде дерева объектов. Это не файл HTML, это структура в памяти браузера.

  Когда браузер получает HTML текст:

  xml

  <html>

  `  `<body>

  `    `<h1>Заголовок</h1>

  `    `<p>Текст</p>

  `  `</body>

  </html>

  Браузер создает дерево:

  text

  html

  └── body

  `    `├── h1

  `    `└── p

  Каждый элемент в этом дереве — это объект с:

- Имя тега (tag name): h1, p, div и т.д.
- Атрибуты: id, class, name и т.д.
- Содержимое (text nodes): текст внутри
- Дети (children): элементы внутри
- Родитель (parent): элемент, который содержит этот элемент
  ## **Почему DOM важна для тестирования?**
  Когда ты пишешь тест Selenium:

1. Ты говоришь Selenium: "найди элемент с id='submit-button'"
1. Selenium ищет этот элемент в DOM
1. Если найденный элемент — Selenium может кликнуть по нему, ввести текст, прочитать значение и т.д.

   Без понимания структуры DOM, сложно писать правильные селекторы.
   ## **Пример: Как браузер создает DOM**
   Исходный HTML:

   xml

   <html>

   `  `<head>

   `    `<title>Моя страница</title>

   `  `</head>

   `  `<body>

   `    `<header>

   `      `<h1>Добро пожаловать</h1>

   `    `</header>

   `    `<main>

   `      `<article>

   `        `<h2>Статья 1</h2>

   `        `<p>Текст статьи</p>

   `      `</article>

   `      `<aside>

   `        `<p>Боковая колонка</p>

   `      `</aside>

   `    `</main>

   `    `<footer>

   `      `<p>© 2026</p>

   `    `</footer>

   `  `</body>

   </html>

   DOM дерево:

   text

   html

   ├── head

   │   └── title

   └── body

   `    `├── header

   `    `│   └── h1

   `    `├── main

   `    `│   ├── article

   `    `│   │   ├── h2

   `    `│   │   └── p

   `    `│   └── aside

   `    `│       └── p

   `    `└── footer

   `        `└── p

   Видишь структуру? Каждый элемент имеет родителя, может иметь детей, может иметь братьев (siblings).
   ## **Отношения в DOM**
   text

   `    `header          (родитель h1)

   `      `│

   `      `└── h1        (дитя header, брат с other\_elements если они есть)

   Родитель-дитя:

- header — родитель
- h1 — дитя

  Братья (siblings):

  xml

  <header>

  `  `<h1>Заголовок</h1>        *<!-- Брат nav, имеет одного родителя header -->*

  `  `<nav>Меню</nav>           *<!-- Брат h1 -->*

  </header>

  Оба элемента находятся внутри одного родителя.

  -----
  ## **БЛОК 2.3: HTML ТЕГИ И АТРИБУТЫ**
  ## **Основные теги**
  Браузер распознает определенные теги и знает, как их отображать. Вот основные:

  xml

  *<!-- СТРУКТУРА -->*

  <html>       *<!-- Корневой элемент -->*

  <head>       *<!-- Информация о странице (не видна) -->*

  <body>       *<!-- Видимое содержимое -->*

  <header>     *<!-- Заголовок страницы -->*

  <footer>     *<!-- Подвал страницы -->*

  <main>       *<!-- Основное содержимое -->*

  <article>    *<!-- Статья -->*

  <section>    *<!-- Секция контента -->*

  <nav>        *<!-- Навигация -->*

  <aside>      *<!-- Боковая колонка -->*

  <div>        *<!-- Универсальный контейнер (нет смысла) -->*

  *<!-- ТЕКСТ -->*

  <h1> до <h6>  *<!-- Заголовки разных уровней -->*

  <p>            *<!-- Абзац -->*

  <span>         *<!-- Встроенный контейнер (нет смысла) -->*

  <b>            *<!-- Жирный текст -->*

  <i>            *<!-- Наклонный текст -->*

  <strong>       *<!-- Важный текст (жирный) -->*

  <em>           *<!-- Выделенный текст (наклонный) -->*

  *<!-- ССЫЛКИ И МЕДИА -->*

  <a>            *<!-- Ссылка -->*

  <img>          *<!-- Изображение -->*

  <video>        *<!-- Видео -->*

  <audio>        *<!-- Аудио -->*

  *<!-- ФОРМЫ -->*

  <form>         *<!-- Форма для ввода -->*

  <input>        *<!-- Поле ввода -->*

  <button>       *<!-- Кнопка -->*

  <textarea>     *<!-- Многострочный текст -->*

  <label>        *<!-- Подпись для input -->*

  <select>       *<!-- Выпадающий список -->*

  <option>       *<!-- Вариант в select -->*

  *<!-- СПИСКИ -->*

  <ul>           *<!-- Неупорядоченный список (точки) -->*

  <ol>           *<!-- Упорядоченный список (цифры) -->*

  <li>           *<!-- Элемент списка -->*

  *<!-- ТАБЛИЦЫ -->*

  <table>        *<!-- Таблица -->*

  <tr>           *<!-- Строка таблицы -->*

  <td>           *<!-- Ячейка таблицы -->*

  <th>           *<!-- Заголовок ячейки -->*

  *<!-- ДРУГОЕ -->*

  <hr>           *<!-- Горизонтальная линия -->*

  <br>           *<!-- Разрыв строки -->*

  *<!-- Комментарий -->*
  ## **Атрибуты — как найти элемент**
  Атрибут — это свойство тега, которое хранит информацию об элементе. Это именно то, что ты используешь в селекторах!

  xml

  <tag attribute1="value1" attribute2="value2">Содержимое</tag>

  Основные атрибуты:
  ## **id — уникальный идентификатор**
  xml

  <button id="login-button">Вход</button>

  <button id="logout-button">Выход</button>

  Важно: каждый id должен быть уникальным на странице. Один id = один элемент.

  xml

  <h1 id="main-heading">Заголовок</h1>

  *<!-- Этот id уже используется! Ошибка! -->*

  <h2 id="main-heading">Еще заголовок</h2>
  ## **class — класс для группировки**
  xml

  <p class="important">Важный текст</p>

  <p class="important">Еще важный</p>

  <p class="warning">Предупреждение</p>

  Отличие от id:

- id — уникален
- class — может быть у нескольких элементов

  Можно использовать несколько классов:

  xml

  <button class="btn btn-primary btn-large">Нажми</button>

  Этот элемент имеет три класса: btn, btn-primary, btn-large.
  ## **name — имя элемента (часто в формах)**
  xml

  <input type="text" name="username">

  <input type="password" name="password">

  Когда форма отправляется, браузер использует name как ключ и значение input как значение.
  ## **type — тип элемента**
  xml

  *<!-- Текстовое поле -->*

  <input type="text">

  *<!-- Email -->*

  <input type="email">

  *<!-- Пароль -->*

  <input type="password">

  *<!-- Чекбокс -->*

  <input type="checkbox">

  *<!-- Радиокнопка -->*

  <input type="radio">

  *<!-- Дата -->*

  <input type="date">

  *<!-- Кнопка в форме -->*

  <button type="submit">Отправить</button>

  <button type="button">Просто кнопка</button>
  ## **placeholder — подсказка в поле**
  xml

  <input type="text" placeholder="Введите ваше имя">

  Когда пользователь фокусируется на поле, подсказка исчезает.
  ## **value — начальное значение**
  xml

  <input type="text" value="Иван">

  <button value="submit">Отправить</button>
  ## **href — ссылка (для тега a)**
  xml

  <a href="https://google.com">Google</a>

  <a href="/about">О нас</a>
  ## **src — источник (для img, video, script)**
  xml

  <img src="photo.jpg">

  <video src="movie.mp4"></video>

  <script src="script.js"></script>
  ## **alt — альтернативный текст для изображения**
  xml

  <img src="photo.jpg" alt="Фото моего кота">

  Если изображение не загрузилось, браузер покажет этот текст.
  ## **data-\* — пользовательские атрибуты**
  Ты можешь создать свой атрибут с префиксом data-:

  xml

  <button data-id="123" data-action="delete">Удалить</button>

  <div data-color="red" data-size="large">Контейнер</div>

  Это удобно для хранения дополнительной информации и поиска элементов в тестах.

  -----
  ## **БЛОК 2.4: КАК БРАУЗЕР ОБРАБАТЫВАЕТ HTML**
  ## **Процесс парсинга (разбора HTML)**
  Когда браузер получает HTML текст, он проходит несколько этапов:

1. Tokenization (Токенизация) — браузер разбивает текст на токены (теги, текст, атрибуты)
1. Tree Construction (Построение дерева) — браузер создает DOM дерево из токенов
1. Rendering (Отрисовка) — браузер рисует страницу на экран
   ## **Пример: как браузер парсит HTML**
   xml

   <html>

   `  `<body>

   `    `<h1>Привет</h1>

   `  `</body>

   </html>

   Этап 1: Tokenization

- Токен 1: <html>
- Токен 2: <body>
- Токен 3: <h1>
- Токен 4: Привет (текст)
- Токен 5: </h1>
- Токен 6: </body>
- Токен 7: </html>

  Этап 2: Tree Construction

  Браузер строит дерево:

  text

  html

  └── body

  `    `└── h1

  `        `└── "Привет" (текстовый узел)

  Этап 3: Rendering

  Браузер отрисовывает:

- html — корневой элемент (весь контент)
- body — видимая область
- h1 — заголовок, браузер знает, что он большой и жирный
- Привет — текст внутри h1

  На экране ты видишь: Привет (большой жирный текст)
  ## **Почему элементы "не видны"?**
  Иногда в тестах ты находишь элемент через селектор, но он "не виден". Почему?

1. CSS скрыл элемент — display: none, visibility: hidden, opacity: 0
1. Элемент за границами экрана — он в DOM, но ты его не видишь
1. Элемент динамически загружается — его нет в исходном HTML, он появляется позже через JavaScript

   Это важно для написания тестов: ты должен ждать, пока элемент станет видимым перед взаимодействием!

   -----
   ## **БЛОК 2.5: CSS-СЕЛЕКТОРЫ**
   Селектор — это способ "указать" на элемент в HTML. В автотестировании селектор используется, чтобы Selenium знал, какой элемент нужен.
   ## **Типы CSS-селекторов**
   ## **1. По тегу**
   xml

   <p>Абзац 1</p>

   <p>Абзац 2</p>

   css

   p

   Найдет все элементы <p>.

   Когда использовать: когда на странице мало таких элементов, или ты хочешь найти все сразу.
   ## **2. По id (уникальный)**
   xml

   <button id="submit-btn">Отправить</button>

   css

   #submit-btn

   Найдет один элемент с id="submit-btn".

   Правило: # перед именем id.

   Когда использовать: когда id есть (это самый надежный селектор, потому что id уникален).
   ## **3. По class**
   xml

   <p class="error">Ошибка</p>

   <p class="error">Еще ошибка</p>

   <p class="warning">Предупреждение</p>

   css

   .error

   Найдет все элементы с class="error".

   Правило: . перед именем class.

   Когда использовать: когда нужно найти несколько элементов с одним классом.
   ## **4. По атрибуту**
   xml

   <input type="text" name="username">

   <input type="email" name="email">

   <input type="password" name="password">

   css

   */\* Найти input с type="text" \*/*

   input[type="text"]

   */\* Найти input с name="username" \*/*

   input[name="username"]

   */\* Найти input с любым атрибутом type \*/*

   input[type]

   Правило: [атрибут="значение"].
   ## **5. Множественный селектор (OR)**
   css

   */\* Найти p ИЛИ span \*/*

   p, span

   */\* Найти элементы с классом error ИЛИ warning \*/*

   .error, .warning
   ## **6. Комбинирование селекторов**
   xml

   <p class="important">Важный абзац</p>

   <span class="important">Важный спан</span>

   css

   */\* Найти p с классом important (не span) \*/*

   p.important
   ## **7. Иерархия: прямое потомство (>)**
   xml

   <div class="container">

   `    `<button>Кнопка внутри container</button>

   </div>

   css

   */\* Найти button, который является прямым потомком div с классом container \*/*

   div.container > button

   /\* Это найдет button внутри div -->

   .container > button
   ## **8. Иерархия: любой потомок (пробел)**
   xml

   <div class="container">

   `    `<section>

   `        `<button>Кнопка глубоко внутри</button>

   `    `</section>

   </div>

   css

   */\* Найти button, который находится где-то внутри container (на любой глубине) \*/*

   .container button

   /\* Это найдет button на любой глубине внутри .container -->

   div button

   Разница между > и пробелом:

- > — только прямые дети
- пробел — все потомки на любой глубине
  ## **9. Псевдо-селекторы**
  css

  */\* Первый элемент типа \*/*

  p:first-child

  */\* Последний элемент типа \*/*

  p:last-child

  */\* Элемент по порядку (n-th) \*/*

  p:nth-child(2)  */\* Второй p \*/*

  */\* Элемент при наведении мышки \*/*

  button:hover

  */\* Активный элемент (при клике) \*/*

  a:active

  */\* Фокус (например, input при клике) \*/*

  input:focus

  */\* Отключенный элемент \*/*

  button:disabled

  */\* Элемент с текстом, содержащим строку \*/*

  p:contains("Привет")  */\* XPath, не CSS \*/*
  ## **Примеры реальных селекторов**
  xml

  *<!-- HTML -->*

  <div class="login-form">

  `    `<input type="text" name="username" placeholder="Имя">

  `    `<input type="password" name="password" placeholder="Пароль">

  `    `<button type="submit" id="login-btn">Вход</button>

  </div>

  css

  */\* Селекторы для этого HTML \*/*

  */\* По id (самый надежный) \*/*

  #login-btn

  */\* По классу родителя + имя тега \*/*

  .login-form button

  */\* По атрибуту type \*/*

  button[type="submit"]

  */\* По атрибуту name \*/*

  input[name="username"]

  */\* Комбинация: input с type и name \*/*

  input[type="text"][name="username"]

  */\* Текстовое поле внутри формы \*/*

  .login-form input[type="text"]

  /\* Все input внутри формы -->

  .login-form input

  /\* Первый input -->

  .login-form input:first-child

  -----
  ## **БЛОК 2.6: XPATH (АЛЬТЕРНАТИВА CSS)**
  XPath — это другой способ найти элемент. Вместо CSS используешь "путь" в DOM дереве.
  ## **XPath vs CSS**

  |**Возможность**|**CSS**|**XPath**|
  | :- | :- | :- |
  |По тегу|✅|✅|
  |По id|✅|✅|
  |По классу|✅|✅|
  |По атрибуту|✅|✅|
  |По тексту|❌|✅|
  |По позиции|⚠️|✅|
  |Логика (AND, OR)|⚠️|✅|
  |Родитель|❌|✅|
  ## **Синтаксис XPath**
  text

  /\* Основная структура \*/

  //tag\_name[@attribute='value']

  /\* Примеры \*/

  /\* Найти все p \*/

  //p

  /\* Найти все p с class="important" \*/

  //p[@class='important']

  /\* Найти первый p \*/

  //p[1]

  /\* Найти button с текстом "Отправить" \*/

  //button[text()='Отправить']

  /\* Найти input с type="text" и name="username" \*/

  //input[@type='text'][@name='username']

  /\* Найти p внутри div с id="container" \*/

  //div[@id='container']//p

  /\* Найти p, которая является прямым потомком div \*/

  //div/p

  /\* Найти родителя элемента \*/

  //button[@id='submit']/.. 

  /\* Найти следующий брат \*/

  //h1/following-sibling::p[1]

  /\* Найти предыдущий брат \*/

  //h1/preceding-sibling::p[1]

  /\* Найти содержит текст (partial) \*/

  //button[contains(text(), 'Отправ')]

  /\* Найти по starts-with \*/

  //input[starts-with(@name, 'user')]

  /\* Найти по ends-with \*/

  //button[ends-with(@class, 'primary')]
  ## **Реальные примеры**
  xml

  *<!-- HTML -->*

  <form id="login-form">

  `    `<label>Имя:</label>

  `    `<input type="text" name="username">

    

  `    `<label>Пароль:</label>

  `    `<input type="password" name="password">

    

  `    `<button type="submit">Войти</button>

  `    `<button type="reset">Очистить</button>

  </form>

  text

  /\* XPath селекторы \*/

  /\* Найти форму по id \*/

  //form[@id='login-form']

  /\* Найти все input внутри формы \*/

  //form[@id='login-form']//input

  /\* Найти кнопку с текстом "Войти" \*/

  //button[text()='Войти']

  /\* Найти кнопку, которая содержит текст "Войти" (partial) \*/

  //button[contains(text(), 'Вой')]

  /\* Найти первый input в форме \*/

  //form[@id='login-form']//input[1]

  /\* Найти input с name="username" в форме \*/

  //form[@id='login-form']//input[@name='username']

  /\* Найти кнопку, которая следует после label "Пароль:" \*/

  //label[text()='Пароль:']/following-sibling::input

  /\* Найти input, чей родитель - форма с id \*/

  //input[parent::form[@id='login-form']]

  /\* Найти button, который содержит "Войти" или "Очистить" \*/

  //button[contains(text(), 'Войти') or contains(text(), 'Очистить')]

  /\* Найти button с type="submit" \*/

  //button[@type='submit']

  /\* Найти все элементы с классом, который содержит "btn" \*/

  //\*[contains(@class, 'btn')]

  -----
  ## **НОВОЕ: БЛОК 2.9 - СРАВНЕНИЕ ИНСТРУМЕНТОВ 🆕**
  ## **Selenium vs Playwright vs Cypress**
  Есть три основных инструмента для автоматизации веб-тестирования. Какой выбрать?
  ## **Таблица сравнения**

  |**Критерий**|**Selenium**|**Playwright**|**Cypress**|
  | :- | :- | :- | :- |
  |Языки|Java, Python, C#, JS, Ruby|Python, JS, Java, .NET|Только JavaScript|
  |Браузеры|Chrome, Firefox, Safari, Edge|Chrome, Firefox, Safari, Edge|Chrome, Firefox, Safari, Edge|
  |Скорость|Медленнее|Быстрее|Очень быстро|
  |Архитектура|Клиент-сервер|Сервер|Встроена в браузер|
  |API|WebDriver|DevTools Protocol|Cypress API|
  |Отладка|Сложнее|Хорошо|Отличная|
  |Мобильное|Да (Appium)|Да|Нет|
  |Параллельное|Есть|Встроено|Встроено|
  |Стоимость|Бесплатно|Бесплатно|Бесплатно (+ платный облако)|
  |Кривая обучения|Средняя|Средняя|Легкая|
  |Production готовность|✅ Проверено годами|✅ Растет популярность|✅ Растет популярность|
  ## **Когда использовать Selenium**
  text

  ✅ Используй Selenium если:

  - Нужна многоязычность (Python, Java, C# и т.д.)

  - Тестируешь мобильные приложения (Appium)

  - Работаешь в legacy проектах

  - Нужна максимальная популярность и сообщество

  text

  ❌ Не используй Selenium если:

  - Нужна максимальная скорость

  - Хочешь самый простой API

  - Хочешь работать только на JavaScript
  ## **Когда использовать Playwright**
  text

  ✅ Используй Playwright если:

  - Нужна быстрая и надежная автоматизация

  - Работаешь с несколькими браузерами

  - Нужна поддержка мобильных эмуляций

  - Хочешь лучший баланс функциональности и скорости

  text

  ❌ Не используй Playwright если:

  - Нужна работа с legacy браузерами

  - Требуется Java или C# (хотя есть биндинги)
  ## **Когда использовать Cypress**
  text

  ✅ Используй Cypress если:

  - Разрабатываешь на JavaScript/TypeScript

  - Нужна максимально простая отладка

  - Работаешь с современными веб-приложениями

  - Нужна хорошая документация для новичков

  text

  ❌ Не используй Cypress если:

  - Нужна поддержка нескольких вкладок/окон (есть ограничения)

  - Нужна многоязычность

  - Нужно тестировать мобильные приложения

  - Нужна поддержка разных доменов в одном тесте
  ## **Примеры кода**
  ## **Selenium (Python)**
  python

  **from** selenium **import** webdriver

  **from** selenium.webdriver.common.by **import** By

  **from** selenium.webdriver.support.ui **import** WebDriverWait

  **from** selenium.webdriver.support **import** expected\_conditions **as** EC

  driver = webdriver.Chrome()

  driver.get("https://example.com")

  *# Найти элемент*

  button = driver.find\_element(By.ID, "submit")

  *# Ждать условие*

  wait = WebDriverWait(driver, 10)

  wait.until(EC.element\_to\_be\_clickable((By.ID, "submit")))

  *# Клик*

  button.click()

  driver.quit()
  ## **Playwright (Python)**
  python

  **from** playwright.sync\_api **import** sync\_playwright

  **with** sync\_playwright() **as** p:

  `    `browser = p.chromium.launch()

  `    `page = browser.new\_page()

  `    `page.goto("https://example.com")

    

  `    `*# Найти элемент и кликнуть (автоматическое ожидание!)*

  `    `page.click("#submit")

    

  `    `*# Ждать навигацию*

  `    `page.wait\_for\_load\_state("networkidle")

    

  `    `browser.close()
  ## **Cypress (JavaScript)**
  javascript

  describe('Example Test', () => {

  `  `it('Should click button', () => {

  `    `cy.visit('https://example.com')

    

  `    `*// Найти элемент и кликнуть (автоматическое ожидание!)*

  `    `cy.get('#submit').click()

    

  `    `*// Проверить результат*

  `    `cy.url().should('include', '/dashboard')

  `  `})

  })

  -----
  ## **НОВОЕ: БЛОК 2.10 - BEST PRACTICES 2024 🆕**
  ## **Современные подходы к выбору локаторов**
  Иерархия надежности локаторов (от лучшего к худшему):

1. ID (если уникален и не меняется)

   python

   *# Лучший вариант*

   find\_element(By.ID, "submit-button")

1. *Атрибут data- (специально для тестов)*\*

   python

   *# Отлично: разработчик специально добавил для тестов*

   find\_element(By.CSS\_SELECTOR, "[data-testid='submit']")

1. CSS селектор с классом

   python

   *# Хорошо*

   find\_element(By.CSS\_SELECTOR, ".btn-submit")

1. XPath с текстом

   python

   *# Работает, но может быть хрупким*

   find\_element(By.XPATH, "//button[text()='Отправить']")

1. Сложные CSS селекторы

   python

   *# Не очень хорошо: много зависимостей*

   find\_element(By.CSS\_SELECTOR, ".form .actions > button:last-child")

1. Сложные XPath

   python

   *# Очень плохо: будет сломано при любом изменении структуры*

   find\_element(By.XPATH, "//div[@class='container']/div[1]/form/button")
   ## **Рекомендации на 2024**
1. Просите разработчиков добавить data-testid

   xml

   *<!-- IDE хорошо: есть data-testid -->*

   <button data-testid="login-submit">Вход</button>

1. Используй CSS селекторы (они быстрее)

   python

   *# Быстрее*

   find\_element(By.CSS\_SELECTOR, "[data-testid='login']")

   *# Медленнее*

   find\_element(By.XPATH, "//button[@data-testid='login']")

1. Избегай индексов в селекторах

   python

   *# Плохо: сломается, если добавится новая кнопка*

   buttons = find\_elements(By.TAG\_NAME, "button")

   buttons[2].click()  *# Третья кнопка*

   *# Хорошо: явный селектор*

   find\_element(By.CSS\_SELECTOR, "[data-testid='submit']").click()

1. Используй Relative Locators (Selenium 4+)

   python

   *# Современный подход (новое в Selenium 4)*

   submit\_button = driver.find\_element(By.ID, "submit")

   password\_field = driver.find\_element(

   `    `By.CSS\_SELECTOR, 

   `    `"input",

   `    `locate\_with=RelativeBy.above(submit\_button)

   )

   -----
   ## **ИТОГО: ЧТО НУЖНО ЗНАТЬ**
1. HTML — разметка, теги, атрибуты
1. DOM — древо объектов в браузере
1. CSS селекторы — способ найти элемент по селектору
1. XPath — альтернативный способ найти элемент
1. Selenium vs Playwright vs Cypress — три главных инструмента
1. Best Practices — как правильно выбирать локаторы
1. Элементы — то, с чем ты работаешь в тестах
   -----
   ## **ПРАКТИЧЕСКИЕ ЗАДАНИЯ**
   ## **Задание 2.1: HTML структура**
   Напиши HTML страницу с:

- Заголовком (h1)
- Параграфом (p)
- Формой с input и button
- Ссылкой (a)

  Добавь id и class атрибуты.
  ## **Задание 2.2: CSS селекторы**
  Для HTML из задания 2.1 напиши CSS селекторы:

- Заголовок
- Кнопка внутри формы
- Все элементы с классом "important"
- Первый input
  ## **Задание 2.3: XPath**
  Напиши XPath селекторы для того же HTML:

- Кнопка с текстом "Отправить"
- Все input внутри формы
- Ссылка с href="/about"
  ## **Задание 2.4: Выбор инструмента**
  Выбери инструмент (Selenium, Playwright или Cypress) для:

- Проекта на Python
- Проекта на JavaScript
- Проекта с мобильным тестированием
- Простого проекта для новичка

  Обоснуй выбор.
  ## **Задание 2.5: Практика локаторов**
  Открой [**https://www.saucedemo.com**](https://www.saucedemo.com/) в браузере.

  Найди селекторы CSS и XPath для:

- Поля username
- Поля password
- Кнопки Login

  Убедись, что селекторы работают в Chrome DevTools.

  === КОНЕЦ ФАЙЛА: module\_2\_web\_tech\_course.md ===
